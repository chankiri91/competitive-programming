package ABC284

fun main() {
    val (N, M) = readLine()!!.split(" ").map { it.toInt() }
    val list = List(M) {
        readLine()!!.split(" ").map { it.toInt() }
    }
    var cnt = 0

    for (i in 0 until M) {
//        if (list[i][0] < list)
    }
}